# Design-Matched Calibration Results

Status: completed outcome-blind screen; no protected association was opened.

The corrected screen completed all 48,000 unique cells with zero non-estimable
rows and selected no eligible finalist. Confirmation therefore did not run,
and the protected image and text associations remain sealed.

The earlier out-of-range scikit-learn seed failure is retained with recovery
provenance. It was rerun with identical scientific settings after normalizing
only the software seed representation. See `TERMINAL_SUMMARY.json`,
`RECOVERY_LOG_2026-08-11.md`, and the canonical screen manifest and ledgers
under `out/screen/`.
